server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/nationwide?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  upload:
    root-dir: ./uploads/
    image-dir: image/
    member-dir: member-images/
    community-dir: community-images/

  jpa:
    open-in-view: false # View 영속성 제거
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect
      default_batch_fetch_size: 100

  #SMPT 이메일 인증
  mail:
    host: smtp.gmail.com # 구글 SMTP 서버 주소
    port: 587 # 구글 SMTP 포트 번호
    username: whwhwjddn123@gmail.com
    password: qbkejbzudkpjxfnw

    properties:
      mail:
        smtp:
          auth: true # 사용자 인증 여부
          timeout: 5000 # 이메일 전송 타임아웃 시간
          starttls:
            enable: true # TLS를 통해 안전한 통신 사용 여부

logging:
  level:
    org.hibernate.SQL: debug

jwt:
  secret: your-very-long-jwt-secret-key-at-least-32-characters
  access-expiration-ms: 3600000
  refresh-expiration-ms: 604800000

board:
  view:
    cooldown-seconds: 1800
